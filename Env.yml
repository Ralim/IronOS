name: "ironos"
services:
  builder:
    stdin_open: true
    tty: true
    build:
      context: .
      dockerfile: scripts/IronOS.Dockerfile
      args:
        UID: ${HOST_UID:-1000}
        GID: ${HOST_GID:-1000}
    command: /bin/sh
    volumes:
      - ./:/build/ironos:Z
