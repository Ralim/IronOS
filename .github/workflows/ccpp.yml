name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: cd
      run: cd workspace/TS100
    - name: run
      run: make -j3 lang=UK model=TS100
