name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: cd /home/runner/work/ts100/ts100/workspace/TS100
    - name: ls
      run: ls
    - name: make
      run: make -j3 lang=UK model=TS100
