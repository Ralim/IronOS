name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: ls
      run: ls
    - name: chmod
      run: chmod +x build.sh
    - name: make
      run: ./build.sh
